#ifndef _APP_DATAFILE_H__
#define _APP_DATAFILE_H__

/** Implemented this due to requirements of mesher files.
 * They are usually flat files with no particular structure
 * listing entities of several types which reference each other
 * accompanied by several attributes for solver itself.
 */

void loadData(std::istream & input, 

#endif

