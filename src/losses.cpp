#include "losses.h"

Losses::Losses(double insideTemp, double outsideTemp, double groundTemp): m_insideTemp(insideTemp), m_outsideTemp(outsideTemp), m_groundTemp(groundTemp) {
}

bool Losses::compute(Building * building) {

}
